higher priority:
- default prop for create table, max_length, primary key
- recommendations: vacuum, PK, spatialindex, geom to geometry_columns
- warnings: no privileges (select / edit), incorrect postgis upgrade

low priority:
- create views
- vacuum, analyze
- import non-spatial tables, e.g. csv/dbf
- add geospatial support to db
- geometry_columns validity check
